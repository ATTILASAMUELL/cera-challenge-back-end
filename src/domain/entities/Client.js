export default class Client {
  constructor({ id, nome, cpf, observacoes, contato, endereco }) {
    this.id = id
    this.nome = nome
    this.cpf = cpf
    this.observacoes = observacoes
    this.contato = contato
    this.endereco = endereco
  }
}
